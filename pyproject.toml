[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kairosys-auditable-research"
version = "0.1.0"
description = "Offline clean-room demonstration of auditable financial-research boundaries"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["demo*"]
