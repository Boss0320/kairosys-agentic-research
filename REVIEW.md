# Review Record

This began as a local clean-room candidate with no license, Git history, remote,
or publication authority. As of 2026-08-29 it carries an evaluation-only LICENSE,
a single-commit Git history, and a private GitHub remote; public visibility remains
a separate, unapproved gate.

| Gate | Verification command | Observed output | Root closed by | Residual risk |
| --- | --- | --- | --- | --- |
| Task 1 source contract | `shasum -a 256 INTERNAL-kairosys-curated-source-manifest.md` was captured in the private Portfolio authority workspace and is deliberately not reproducible from this isolated public candidate. | `efeaf3ceb0dc29a5e31468cf046c0dc83f21b817ec36053c44a8799be4c3ec64` identifies the immutable Task 1 manifest reviewed privately. | The private source contract is excluded by design; this candidate retains only its derived default-deny boundary. | The private contract remains review evidence, not a candidate file, publication grant, or import permission. |
| Task 2 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_public_boundary -v` | `ModuleNotFoundError: No module named 'scripts.check_public_boundary'` before implementation. | A failing import demonstrated that the new scanner behavior was not already present. | This only proves the initial absence of the module. |
| Task 2 focused hardening tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_public_boundary -v` | `Ran 20 tests in 0.010s` followed by `OK`. | Tests exercise blocked labels, paths, credential assignments including terminal-sensitive forms, overclaims, files, caches, roots, symlinks, and allowed clean-room language. | Pattern matching cannot replace a human review of novel disclosure risks. |
| Task 2 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The scanner checks the candidate shell, including its own test construction. | A clean local scan does not grant a remote, license, publication, or release decision. |
| Task 3 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_serialization -v` | `ModuleNotFoundError: No module named 'demo'` before the frozen domain package was implemented. | The failing import proved the serialization contract was not already present. | This proves only the initial absence of the package, not every later behavior. |
| Task 3 focused serialization tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_serialization -v` | `Ran 4 tests in 0.000s` followed by `OK`. | Tests exercise the exact frozen dataclass field surface, immutable instances, frozen enum values, byte-stable JSON, Decimal strings, enum values, tuple arrays, and recursively sorted mappings. | The clean-room slice accepts only its declared domain values; later pipeline stages must validate their own scenario semantics. |
| Task 3 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The default-deny scanner evaluated the completed candidate tree. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 3 Fix Round 1 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_serialization -v` | `AssertionError: TypeError not raised` for a nested mapping containing an integer key. | The regression test exposed that recursive serialization left non-string keys for JSON to coerce. | This proof covers the specified non-string-key path, not arbitrary future serialization types. |
| Task 3 Fix Round 1 focused serialization tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_serialization -v` | `Ran 6 tests in 0.001s` followed by `OK`. | Tests require deterministic rejection of nested non-string mapping keys and lock every dataclass annotation with `get_type_hints()`, including nullable Decimal values, tuple element types, and separate deterministic/model provenance fields. | Future additions must intentionally expand the frozen contract and its tests. |
| Task 3 Fix Round 1 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The default-deny scanner evaluated the candidate after the Fix Round 1 evidence was added. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 4 provenance RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_provenance -v` | `ModuleNotFoundError: No module named 'demo.kairosys_case.provenance'`; `Ran 1 test`; `FAILED (errors=1)`. | The requested provenance module did not exist before the tests were written. | RED proves initial absence, not all later behavior. |
| Task 4 scenario RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_scenarios -v` | `ModuleNotFoundError: No module named 'demo.kairosys_case.scenarios'`; `Ran 1 test`; `FAILED (errors=1)`. | The requested scenario catalog did not exist before its behavioral tests were written. | RED proves initial absence, not all later behavior. |
| Task 4 focused authority and scenario tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_provenance tests.test_scenarios -v` | `Ran 13 tests in 0.000s` followed by `OK`. | Tests rebuild trusted entries from valid non-null tool observations, discard all model claims while counting them, preserve conflicts, enforce the exact sort key, and lock five immutable synthetic inputs with their required evidence and draft shapes. | Later tasks remain responsible for financial-state, valuation authorization, and rendered-report audit decisions. |
| Task 4 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The completed candidate tree passed the default-deny public-boundary scanner. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 4 Fix Round 1 focused hardening tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_provenance tests.test_scenarios -v` | `Ran 17 tests in 0.000s` followed by `OK`. | Regression tests prove model-claim mappings are never read, valid observations require a value, missing/invalid/conflict observations remain excluded, equal trusted sort keys preserve input order without a value tie-break, and every fixture has exact approved drafts and quarter periods. | This is regression coverage of existing behavior; future policy stages remain outside this task. |
| Task 4 Fix Round 1 self-scan | `python3 scripts/check_public_boundary.py .` | Pending final scan after this append. | The final scanner run below confirms the completed candidate tree remains within its default-deny boundary. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 4 Fix Round 1 final self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The completed hardening tests and their append-only evidence leave the candidate within its default-deny boundary. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 5 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_valuation -v` | `ModuleNotFoundError: No module named 'demo.kairosys_case.valuation'`; `Ran 1 test in 0.000s`; `FAILED (errors=1)`. | The requested valuation module did not exist before the state-matrix and authorization tests were written. | RED proves the initial absence of this module, not later behavior. |
| Task 5 focused financial-state and authorization tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_valuation -v` | `Ran 10 tests in 0.000s` followed by `OK`. | Tests lock all eight approved state/reason rows; cross-year newest-first ordering; malformed-period and missing-EPS fail-closed reasons; rejection of bool, int, and float financial values; mixed-sign Decimal EPS sum semantics; and all four public authorization rows. The implementation requires exactly four distinct consecutive `YYYYQ[1-4]` periods, keeps missing values explicit, uses Decimal EPS arithmetic, and only authorizes a synthetic target for stable profit. | Future pipeline stages must independently validate source evidence and any later draft rendering; this module classifies synthetic records and does not calculate a valuation. |
| Task 5 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The completed clean-room candidate passed its default-deny public-boundary scanner. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 6 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_report_audit -v` | `ModuleNotFoundError: No module named 'demo.kairosys_case.report_audit'`; `Ran 1 test in 0.000s`; `FAILED (errors=1)`. | The requested rendered-report audit module was absent before behavioral tests were written. | RED proves initial module absence, not later audit coverage. |
| Task 6 focused audit and delivery tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_report_audit -v` | `Ran 9 tests in 0.000s` followed by `OK`. | Tests run the four narrow label checks and all delivery meanings: unsupported claimed metrics review with exact line evidence; unauthorized targets block and become context-only while target and displayed P/E lines are removed; conflicting EPS and inconsistent or malformed P/E block; review/block reports keep a visible label with a `0.40` cap; insufficient empty input is withheld; clean input remains editable. | This is intentionally only four explicit labels, not a general Markdown verifier; the downstream pipeline remains responsible for copying the returned cap into its serialized audit result. |
| Task 6 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The completed candidate tree, including the new narrow report audit and tests, passed the default-deny public-boundary scanner. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 6 Fix Round 1 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_report_audit -v` | `Ran 16 tests in 0.002s`; `FAILED (failures=1, errors=2)`. The failures were: a trailing-token target line produced no `RPT-MATH`; identical duplicate Forward EPS lines produced no recomputed `RPT-MATH`; and `100 / 3` rendered as `33.3x` was incorrectly blocked. | New behavioral regressions exercised the real audit module and exposed suffix loss, duplicate-value bypass, and unrounded arithmetic comparison. | This RED covers the named audit gaps before the fix, not future report labels. |
| Task 6 Fix Round 1 focused audit and delivery tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_report_audit -v` | `Ran 17 tests in 0.001s` followed by `OK`. | The audit captures the full suffix of every approved numeric label and fail-closes blank, trailing-token, and non-finite values with exact original-line evidence. Conflicting valid EPS values block without selecting one; identical valid duplicates canonically recompute math. Target/EPS is rounded `ROUND_HALF_UP` to the displayed P/E scale before comparison. Tests lock the ordered four-finding vector and evidence, `100/3 -> 33.3x`, adjacent failure, and RPT-AUTH context-only priority over other blocks. | This remains a deliberately narrow four-label audit; it cannot interpret arbitrary Markdown or later rendering semantics. |
| Task 6 Fix Round 1 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The candidate, new regression tests, and appended evidence pass the default-deny boundary scanner. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 7 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_pipeline -v` | `ModuleNotFoundError: No module named 'demo.kairosys_case.pipeline'`; `Ran 1 test in 0.000s`; `FAILED (errors=1)`. | The end-to-end contract was written before the pipeline module existed, proving the requested orchestration interface was initially absent. | This RED proves initial absence only; it does not prove later scenario behavior. |
| Task 7 focused pipeline and serialization tests | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_pipeline tests.test_serialization -v` | `Ran 17 tests in 0.442s` followed by `OK`. | Tests lock all five delivery outcomes; provenance spoofing; target stripping; contradiction withholding; copied `0.40` policy cap in a new frozen audit result; repeated JSON bytes; CLI choices, rejection before evaluation, stable one-line output; offline AST/import restrictions across all six required modules; and an `open`-patched ready evaluation. | The AST gate intentionally permits the existing `re.compile` regex constructor while blocking bare or explicit `builtins` dynamic compilation; it is not a whole-language sandbox. |
| Task 7 direct CLI pair evidence | `for name in ready_report spoofed_provenance incomplete_financials contradictory_financials rendered_math_conflict; do first="$(python3 -m demo.kairosys_case.cli --scenario "$name")"; second="$(python3 -m demo.kairosys_case.cli --scenario "$name")"; test "$first" = "$second"; printf '%s pair_bytes_equal=true bytes=%s\\n' "$name" "$(printf %s "$first" | wc -c | tr -d ' ')"; done` | All five pairs reported `pair_bytes_equal=true`: ready `1774`, spoofed `1813`, incomplete `780`, contradictory `587`, math-conflict `2025` bytes. | The thin argparse adapter produced one deterministic JSON object for each approved scenario; each separately executed same-name pair matched byte-for-byte. | This verifies the five fixed synthetic cases, not unapproved input names beyond argparse rejection or external data paths. |
| Task 7 self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The new pipeline, CLI, test coverage, and this append remain inside the candidate's default-deny public boundary. | A clean local scan does not authorize a remote, license, publication, or release. |
| Task 8 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `ModuleNotFoundError: No module named 'scripts.build_web_data'`; `Ran 1 test in 0.000s`; `FAILED (errors=1)`. | The generated-data contract existed before the requested browser generator, proving the new interface was initially absent. | RED proves initial module absence only; it does not prove the completed browser explorer. |
| Task 8 deterministic browser-data and contract tests | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/build_web_data.py web/data.js` then `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `Ran 5 tests in 0.002s` followed by `OK`. The second generation retained `e4968d2bc516e226f049bab2854544e0d80c23f763f800a584ad2392db597d3c` for `web/data.js`. | Tests lock all five approved scenario payloads against stable pipeline JSON, compact sorted JavaScript with the exact global prefix/suffix and no generated metadata, file parity, semantic single-main/single-h1/offline/accessibility shell, bilingual `COPY`, mobile and reduced-motion CSS hooks, and an app source free of recomputed authorization/provenance/delivery policy operations. | Browser layout and interaction have not yet been visually inspected in a real browser; local generated data and source contracts do not replace browser QA. |
| Task 8 JavaScript and public-boundary checks | `node --check web/app.js`; `node --check web/data.js`; `python3 scripts/check_public_boundary.py .` | Both Node syntax commands exited `0` with no output; boundary scanner returned `[]`. | The browser scripts parse and the Task 8 files remain inside the clean-room scanner boundary. | Syntax and static scanning do not verify rendering, assistive-technology behavior, remote publication, or release authority. |
| Task 8 Fix Round 1 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `Ran 7 tests in 0.004s`; `FAILED (failures=2)`. The first new contract found no `result.provenance.entries` raw-field rendering and the second found no `result.provenance.discarded_model_claims` conditional strike decision. | The new contracts exposed that the tool card mislabeled a scenario identifier as tool output and every model-claim card was crossed out regardless of evidence. | RED covers the requested evidence-rail truthfulness paths, not browser visual rendering. |
| Task 8 Fix Round 1 evidence-rail contracts | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `Ran 7 tests in 0.003s` followed by `OK`. | Contracts lock five raw discarded-claim counts—only `spoofed_provenance` is `1`; the other four are `0`—and require the tool card to render `provenance.entries` using raw source tool, metric, value, period, and unit fields. They also require the class expression to derive from `discardedCount > 0`, with distinct localized discarded and empty messages. | Source contracts do not replace a real-browser check of the crossed-out visual state, line wrapping, or locale switching. |
| Task 8 Fix Round 1 JavaScript and boundary checks | `node --check web/app.js`; `node --check web/data.js`; `python3 scripts/check_public_boundary.py .` | Both Node syntax commands exited `0` with no output; boundary scanner returned `[]`. | The rendering-only edit parses and remains inside the clean-room public boundary. | These checks do not authorize publication, deployment, or external resources. |
| Task 8 Fix Round 2 Node DOM-stub runtime contract | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `Ran 8 tests in 0.053s` followed by `OK`. The new test executes the generated `web/data.js` and `web/app.js` together inside Node `vm` with a minimal DOM stub. | Runtime assertions click every one of the five generated scenario buttons; compare the resulting tool card to each scenario's raw `provenance.entries`; require `discarded-claim` exactly when the raw counts are `0, 1, 0, 0, 0`; and click the actual `zh-TW` control to exercise its zero and positive model-card messages. This closes the prior source-only evidence-rail contract gap without changing UI code. | A DOM stub exercises rendering logic but is not browser visual QA, layout measurement, or assistive-technology verification. |
| Task 8 Fix Round 2 JavaScript and boundary checks | `node --check web/app.js`; `node --check web/data.js`; `python3 scripts/check_public_boundary.py .` | Both Node syntax commands exited `0` with no output; boundary scanner returned `[]`. | The unchanged browser scripts parse and the candidate remains within the clean-room public boundary after adding the runtime contract. | These local checks do not authorize publication, deployment, or external resources. |
| Task 9 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_content_contract -v` | `Ran 6 tests in 0.003s`; `FAILED (errors=6)`. Each error was a missing public artifact: README, article, or SVG path. | The content contract was written before the bilingual case writing and diagrams existed. | RED proves the requested public content was initially absent, not later visual quality. |
| Task 9 bilingual content contract | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_content_contract -v` | `Ran 6 tests in 0.006s` followed by `OK`. | The contract checks recruiter-first English and Traditional Chinese openings, evidence-before-caveat ordering, equal reading-layer navigation, flagship authority and retained-draft concepts, evolution lineage, overclaim exclusions, and accessible diagram structure. | Content tests cannot measure reader comprehension or visual line wrapping in a browser. |
| Task 9 local link integrity | `python3 scripts/check_links.py .` | `Checked 37 links: 0 broken.` | The default-deny checker resolves relative Markdown links and HTML `src`/`href` targets inside the candidate and rejects absolute, external, escaping, missing, and symlink targets. | This run validates the local tree only; it does not validate a future hosting path. |
| Task 9 SVG XML parse | `python3 -c 'from xml.etree import ElementTree as ET; ET.parse("assets/architecture.svg"); ET.parse("assets/evolution.svg"); print("SVG XML: 2 parsed")'` | `SVG XML: 2 parsed`. | Both fixed-viewBox dossier diagrams parse as XML and include their tested accessibility surface. | XML validity does not prove visual layout at desktop or 390px. |
| Task 9 public-boundary self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The completed bilingual prose, diagrams, content contract, and link checker remain within the candidate's default-deny scanner boundary. | A clean local scan does not authorize a remote, license, publication, deployment, or browser-render acceptance. |
| Task 9 Fix Round 1 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_content_contract -v` | `Ran 10 tests in 0.006s`; `FAILED (failures=4, errors=1)`. The new regressions exposed a 1200-wide SVG effective text size of `9.1`, missing six-section flagship structure, a missing retained math-draft meaning, leaked SVG XLink coverage (`4 != 5`), and DOCTYPE acceptance. | New contracts were added before the fixes. The one test-helper `StopIteration` was corrected to a deterministic assertion failure before implementation. | RED proves the named classes were absent; it does not assess real-browser layout. |
| Task 9 Fix Round 1 hostile SVG-link probe | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_content_contract.ContentContractTests.test_svg_link_checker_rejects_decoded_xlink_and_escape_variants tests.test_content_contract.ContentContractTests.test_svg_link_checker_fails_closed_on_doctype -v` | `Ran 2 tests in 0.004s` followed by `OK`. The temporary hostile SVG produces five inspected targets: plain absolute local, percent-encoded absolute local, parent escape, symlink target, and a numeric-entity XLink href that XML decoding exposes as absolute local; DOCTYPE produces `doctype_not_allowed`. | SVG parsing now rejects DOCTYPE, fails closed on XML parse errors, extracts plain and namespaced `href` attributes after entity decoding, and checks symlinks before resolution. | The checker validates local references only; it is not a general XML security sandbox. |
| Task 9 Fix Round 1 full content and geometry contract | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_content_contract -v` | `Ran 10 tests in 0.005s` followed by `OK`. | Contracts now enforce equal five-scenario ID sets, exact bilingual link destination sets, aligned command blocks, exact delivery meanings including retained math drafts, six ordered flagship sections, forbidden overclaims, caveat ordering, and every SVG text node's `font-size * 390 / viewBox_width >= 16` floor. | Static geometry cannot prove browser font metrics, visual hierarchy, or assistive behavior. |
| Task 9 Fix Round 1 local link and SVG checks | `python3 scripts/check_links.py .`; `python3 -c 'from xml.etree import ElementTree as ET; ET.parse("assets/architecture.svg"); ET.parse("assets/evolution.svg"); print("SVG XML: 2 parsed")'` | `Checked 37 links: 0 broken.` and `SVG XML: 2 parsed`. | The new vertical fixed-width SVGs parse, and all candidate references remain local and resolvable. | No browser desktop or 390px visual QA was performed. |
| Task 9 Fix Round 1 public-boundary self-scan | `python3 scripts/check_public_boundary.py .` | `[]` and exit status `0`. | The checker, test fixtures, rewritten SVGs, and bilingual copy remain inside the default-deny public boundary. | A clean local scan does not authorize a remote, license, publication, deployment, or browser-render acceptance. |
| Task 10 assembly RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_public_boundary -v` | `ModuleNotFoundError: No module named 'scripts.assemble_public'`; `Ran 1 test`; `FAILED (errors=1)`. | The assembly regressions were imported before the manifest assembler existed, proving the requested interface was initially absent. | RED proves initial absence only, not the completed assembly behavior. |
| Task 10 manifest-only assembly regressions | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_public_boundary -v` | `Ran 37 tests in 0.029s` followed by `OK`; 17 assembly tests passed alongside the 20 scanner tests. | Real temporary trees exercise an exact successful copy while leaving undeclared source material behind, and reject duplicate, case-ambiguous, absolute, traversal, symlinked file/parent/manifest/destination, missing, residual, nested-destination, malformed, unsorted, non-self-listing, external-manifest, non-UTF-8, unsupported-type, and malformed-SVG inputs. | This is a local filesystem contract; it cannot rule out a hostile process racing the source during assembly. |
| Task 10 full local suite | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | `Ran 119 tests in 0.547s` followed by `OK`. | The manifest and assembler regressions pass together with every prior pipeline, content, browser-data, provenance, audit, serialization, scenario, and valuation contract. | Automated contracts do not replace real-browser visual or assistive-behavior QA. |
| Task 10 deterministic data and local candidate gates | `PYTHONDONTWRITEBYTECODE=1 python3 scripts/build_web_data.py web/data.js` twice; `python3 scripts/check_links.py .`; `python3 scripts/check_public_boundary.py .`; `node --check web/app.js`; `node --check web/data.js`; exact manifest/tree comparison | Both generations retained SHA-256 `e4968d2bc516e226f049bab2854544e0d80c23f763f800a584ad2392db597d3c`; links reported `Checked 37 links: 0 broken.`; boundary output was `[]`; both Node checks exited `0`; all `44` files exactly matched the manifest. | Deterministic generation, local-reference validation, default-deny scanning, JavaScript parsing, and a literal sorted file-list comparison close the working-tree reproducibility boundary. | These are local static and runtime-contract gates, not browser, legal, publication, remote, or deployment acceptance. |
| Task 10 manifest-only fresh-copy reproduction | `python3 scripts/assemble_public.py --source . --destination <empty fresh directory> --manifest PUBLIC-MANIFEST.txt`, then the full test, two deterministic builds, link, boundary, Node, and literal manifest/tree gates inside that copy | `Assembled 44 files.`; `Ran 119 tests in 0.542s` followed by `OK`; both data hashes were `e4968d2bc516e226f049bab2854544e0d80c23f763f800a584ad2392db597d3c`; `Checked 37 links: 0 broken.`; boundary `[]`; both Node checks exited `0`; `Fresh copy exact: 44 files match manifest.` | The checks execute only the manifest-selected bytes in a new empty directory and reproduce working-tree behavior without relying on undeclared source files. | Browser QA and independent adversarial publication review remain separate gates; there is still no license, Git, remote, push, public visibility, or deployment authority. |
| Task 10 Browser-QA Fix Round 1 RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `Ran 10 tests in 0.063s`; `FAILED (failures=1, errors=1)`: the `900px` tablet block was missing and the shared controls had no `min-height`. | Structured CSS contracts were added before the style change and exposed the exact tablet-column and pointer-target gaps reported by browser QA. | The first control assertion errored on the absent declaration, so RED proves the contract was missing but does not measure rendered pixels. |
| Task 10 Browser-QA Fix Round 1 focused style contracts | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` | `Ran 10 tests in 0.063s` followed by `OK`. | A balanced-block CSS parser now verifies that desktop retains five columns, `max-width: 900px` collapses the rail to one column and removes inter-column borders, `max-width: 390px` retains the compact numbered layout, and language/scenario controls retain `44px` minimum targets. | Structured CSS assertions cannot establish actual computed layout, browser font metrics, focus behavior, or pointer geometry. |
| Task 10 Browser-QA Fix Round 1 local and manifest-only reproduction | Full local suite; two bytecode-disabled data builds; links, boundary, and two Node checks; manifest-only assembly into an empty fresh directory; then the same gates in that copy | Local: `Ran 121 tests in 0.557s`, `OK`; fresh: `Assembled 44 files.`, `Ran 121 tests in 0.555s`, `OK`; all four data hashes were `e4968d2bc516e226f049bab2854544e0d80c23f763f800a584ad2392db597d3c`; each link run checked `37` with `0` broken; each boundary run returned `[]`; Node checks exited `0`; both trees contained exactly `44` manifest files. | The smallest CSS change is reproduced from the manifest with every prior behavioral contract intact and no generated residue. | Root must re-run real-browser QA at all three viewports; this evidence does not authorize browser, publication, license, Git, remote, push, or deployment acceptance. |
| Task 10 real-browser responsive and interaction QA | Serve only the manifest-assembled fresh copy on localhost; inspect and click it in the in-app browser at `1440x1000`, `768x1024`, and `390x844`; exercise all five English scenario tabs, the Traditional Chinese switch, and the spoofed-provenance state; inspect computed geometry and browser console. | `1440x1000`: five `231.6px` evidence columns, no horizontal overflow, controls `44px`. `768x1024`: five stacked `726px` cards, zero right borders, no clipping or horizontal overflow. `390x844`: compact `30px + 297px` numbered cards, column report layout, no clipping or horizontal overflow, controls at least `44px`. All five tabs produced their exact `withheld`, `context_only`, `editable_draft`, or `review_required` text verdicts; only `spoofed_provenance` struck one model claim; zh-TW retained four sections, five tabs, five evidence cards, and the same scenario identity. Main-page console warnings/errors: `[]`. | Real computed layout and clicks close the prior unreadable tablet-column finding while proving the desktop and mobile cascades were not regressed. Text verdicts, empty-report wording, `REVIEW REQUIRED`, and the finding ledger make delivery states non-color-only. | Native controls remain in logical DOM focus order, but synthetic Tab input did not advance focus in the automation surface; no separate screen-reader or assistive-technology session was run. |
| Task 10 real-browser SVG QA | Open `architecture.svg` and `evolution.svg` directly from the same fresh candidate at desktop and `390x844`; inspect screenshots, accessibility metadata, viewBox, computed text sizes, and horizontal overflow. | Architecture: `22` text nodes, title/description/`role=img`, `viewBox=0 0 390 1320`; evolution: `18` text nodes, title/description/`role=img`, `viewBox=0 0 390 1120`. At 390px every computed text size was at least `16px`; both diagrams remained legible with no horizontal overflow. | The vertical 390-unit diagrams close the original compressed 1200-unit layout and preserve the five authority steps, four delivery meanings, and five-stage engineering lineage at both widths. | This verifies the SVGs in a browser, not every downstream Markdown renderer or future hosting wrapper. |
| Task 10 independent publication review | Fresh read-only review of the exact Task 10 source snapshot, review diff, live candidate, and manifest-only fresh copy; rerun full local/fresh tests, deterministic data, links, boundary, Node, manifest inventory, byte equality, and the six adversarial public-story questions. | Initial Task 10 review: `PASS` with `119` local and fresh tests, `44/44` exact files, and no Critical/Important/Minor finding. Browser-fix re-review: `PASS [HIGH]` with `121` local and fresh tests; diff SHA-256 `aeff3348b54c385099729e63edc3a3893e289c57556248ee51a97685accd0340`; manifest unchanged; all `44` candidate, snapshot, and fresh files byte-identical. | Independent review found a recruiter-first ambitious system story, explicit synthetic boundaries, no customer/live/alpha/mature-quality overclaim, accurate solo ownership, no reconstructible private prompt/provider/tuned-rule/control topology, and preserved retained-draft/manual-review nuance. | Factual authority remains the frozen internal source contract rather than a live-product probe. License, Git, remote, push, public visibility, deployment, and current-product/live-report claims remain separately unauthorized. |
| Shared hero hierarchy RED proof | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract.WebShellContractTests.test_hero_separates_product_descriptor_thesis_and_ownership_in_both_languages -v` | `FAILED (failures=1)` because `id="hero-descriptor"` was absent from the old shell. | The new contract was run before production edits and requires four ordered, localized layers: product name, system descriptor, memorable thesis, and ownership. | RED proves the old hero shape lacked the approved hierarchy; it does not assess rendered typography. |
| Shared hero hierarchy focused and local regression gates | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract.WebShellContractTests -v`; `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v`; `python3 scripts/check_links.py .`; `python3 scripts/check_public_boundary.py .`; `node --check web/app.js`; `node --check web/data.js` | Focused shell: `Ran 8 tests in 0.054s`, `OK`; full local: `Ran 122 tests in 0.487s`, `OK`; links: `Checked 37 links: 0 broken.`; boundary: `[]`; both Node checks exited `0`. | The shell contract locks `Kairosys` as both locale H1 values, distinct descriptor/thesis/ownership copy, ordered DOM nodes, bilingual runtime rendering, the unchanged evidence rail, and the existing offline policy boundary. | Static and DOM-stub gates do not measure real font wrapping or responsive hierarchy. |
| Shared hero hierarchy manifest-only reproduction | `python3 scripts/assemble_public.py --source . --destination /private/tmp/kairosys-public-candidate.hero.LmTbOg --manifest PUBLIC-MANIFEST.txt`, then full tests, links, boundary, Node checks, two generated-data builds to temporary files, exact manifest-byte comparison, file count, and symlink scan inside the fresh copy | `Assembled 44 files.`; fresh `Ran 122 tests in 0.492s`, `OK`; links `37/0`; boundary `[]`; Node checks exited `0`; all three data hashes were `e4968d2bc516e226f049bab2854544e0d80c23f763f800a584ad2392db597d3c`; all `44` manifest files were byte-identical; both candidate and fresh copy had zero symlinks. | The changed hero bytes reproduce through the default-deny manifest without undeclared files or changing deterministic case data. | This is a local candidate only; it grants no license, Git, remote, publication, or deployment authority. |
| Shared hero hierarchy real-browser QA | Serve only `/private/tmp/kairosys-public-candidate.hero.LmTbOg/web` on `127.0.0.1`; inspect English and Traditional Chinese at `1440x1000` and `390x844`; measure the four hero nodes, horizontal overflow, DOM hierarchy, and browser warnings/errors. | At both viewports and in both locales, the ordered descriptor, `Kairosys` H1, thesis, and ownership nodes were visible and non-overflowing; document width equaled viewport width. Desktop H1 was `107.2px`; mobile H1 was `51.2px`. Browser warning/error log was `[]`. | Real screenshots and computed geometry confirm that the product name reads as the title while the system description, thesis, and solo ownership read as subordinate copy on desktop and mobile. | No separate screen-reader session or external-browser matrix was run; publication remains unauthorized. |
| Capability-governance core RED and GREEN | RED: `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_quality_governance -v`; GREEN: the same command, then `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_quality_governance tests.test_report_audit.DeliveryPolicyTests tests.test_pipeline tests.test_scenarios tests.test_serialization -v`. | RED raised `ImportError` for the absent typed symbols and ran `1` failing test. Core GREEN ran `6` tests; integrated GREEN ran `43` tests; both ended `OK`. | Immutable typed results, six fixed utility dimensions, sorted unique blocking reasons, insufficient-state failure, hard effective-score cap, utility-only review, six scenarios, and deterministic serialization close the two-layer behavior root. | Completed dimensions are typed upstream input. The public package demonstrates admission semantics, not the complete research runtime. |
| Browser admission RED and GREEN | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_web_contract -v` before and after implementation; `node --check web/app.js`; `node --check web/data.js`. | RED ran `13` tests and failed with `5` failures plus `1` error for stale five-case data, old descriptor, absent overview, and absent quality strip. GREEN ran `13` tests, `OK`; both Node commands exited `0`. | Contracts require six exact payloads, bilingual capability copy, ordered semantic sections, raw supplied gate/utility/effective/delivery values, and no browser-side score or policy recomputation. | DOM-stub tests do not by themselves establish browser font metrics or screen-reader behavior. |
| Recruiter-first content RED and GREEN | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_content_contract -v`; link, boundary, and SVG parse checks. | RED ran `17` tests with `5` failures and `1` error for the narrow story, missing sixth case, absent articles, and old diagram. GREEN ran `17` tests, `OK`; links reported `43` checked and `0` broken; two SVGs parsed; boundary returned `[]`. | English and Traditional Chinese now lead with six research lenses, align six delivery meanings and navigation, explain three distinct admission cases, and preserve the claim boundary. | Static content contracts cannot measure recruiter comprehension or every downstream Markdown renderer. |
| Adversarial self-attack RED and closure | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest` with the three new focused regressions before and after the fix; real 390px SVG geometry probe. | The focused attack first produced `3` failures: mutable weights, an editable empty report, and a forged quality PASS bypassing a blocking audit. After the fix all `3` passed. Browser QA then found one SVG line outside its box; final geometry measured text right `284` within box right `302`. | `MappingProxyType` freezes weights; blank artifacts are withheld; delivery independently rechecks insufficient state and blocking findings; the SVG decision-trace label is split across lines. | Utility coverage still depends on typed upstream completion attestations; this limitation is stated in both governance articles. |
| Final local and manifest-only reproduction | Local and fresh `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests`; two fresh data builds and hashes; links; boundary; Node; SVG; exact manifest/byte/symlink/cache comparison. | Local and fresh each ran `142` tests, `OK`. Both data hashes were `68ed18aa86bb66e8c8602cc716aefe643cb4a2fa4c15d12f18aa1e405e3f25ae`; links `43/0`; boundary `[]`; Node clean; two SVGs parsed; all `48` files were byte-identical with no symlink or bytecode-cache residue. | The final candidate reproduces exclusively from the sorted default-deny manifest and does not depend on undeclared files. | These local gates grant no Git, remote, license, publication, or deployment authority. |
| Final real-browser responsive and interaction QA | Serve only manifest-assembled fresh bytes on localhost; inspect `1440x1000`, `768x1024`, and `390x844`; click all six English cases and the Chinese shallow case; inspect computed geometry and fresh-page logs. | All viewports had `overflowX=false`; controls were at least `44px`. Capabilities rendered `3`, `2`, then `1` column; the evidence rail rendered `5` desktop columns then a readable stack; the mobile quality strip was `368px` wide. Exact outcomes were editable, review, context-only, or withheld as specified; math conflict showed `FAIL / 1.00 / 0.40`, shallow showed `PASS / 0.35 / 0.35`; Chinese preserved the same values. Fresh-page warnings/errors were `[]`. | Real layout and interaction close the static-only gap while proving supplied policy values stay visible and non-color-only. | No separate screen-reader or external-browser matrix was run. Localhost QA does not authorize release. |
| Private remote and initial push (2026-08-29) | Boss-authorized execution: per-repository Git identity `Titus Lai`; evaluation-only `LICENSE`; a single initial commit pushed to the private GitHub remote `kairosys-agentic-research`; full-history `gitleaks` 8.30.1 content scan; post-push GitHub Markdown QA of both READMEs, navigation links, and the SVG diagrams | The private remote holds the single-commit history; gitleaks reported no leaks; GitHub rendering QA passed | Closes the pre-Git "no license, Git history, remote" status above and the post-push rendering gate | The repository stays private; the public toggle and an independent external review before publication remain open Boss gates |
| README demonstration-block fix (2026-08-29) | The published block told a reader to run the boundary scanner directly inside a clone, where its default-deny rules necessarily flag Git metadata and freshly created runtime caches; both READMEs now assemble the manifest candidate first and scan that copy. Re-ran `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests`, `python3 scripts/check_links.py .`, and the exact new block from a working tree with Git metadata and runtime caches present. | `Ran 144 tests`, `OK`; `Checked 45 links: 0 broken.`; `Assembled 49 files.`; the boundary scan of the assembled candidate returned `[]` with exit `0`. | The reader-facing demonstration now passes in a real Git clone while the scanner stays default-deny. | The block is verified locally on macOS; the shell substitution assumes a POSIX shell. |
